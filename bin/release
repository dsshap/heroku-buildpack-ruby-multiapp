#!/usr/bin/env ruby

$:.unshift File.expand_path("../../lib", __FILE__)
require "language_pack"

if pack = LanguagePack.detect(ARGV[0], ARGV[1])
  Kernel.puts "about to be in release"
  $stdout.flush
  puts "about to be in release 2"
  puts pack.release
end

